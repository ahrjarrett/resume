const primary = "hsl(151,86%,34%)"
const accent = "hsl(151,62%,79%)"

const primaryLite = "hsl(151,50%,82%)"
const black = "hsl(151,50%,0%)"
const text = black
const header = black

export const theme = {
  primary,
  accent,
  primaryLite,
  black,
  text,
  header
}
